services:
  - type: web
    name: n8n-video-editor
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
