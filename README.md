# n8n-video-editor
A custom, free-forever Python video generator. It receives raw edit requests from n8n, uses the open-source MoviePy and FFmpeg engines to automatically compile and edit videos, and returns the finished video link back to your workflow.
